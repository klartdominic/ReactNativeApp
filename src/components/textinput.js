import React from 'react';

const customTextInput = ({title}) => {
  return{ 
    <TextInput
      style={styles.textInput}
      placeholder="Todo Title"
      selectionColor="#fff"
      underlineColor={this.state.backgroundColor}
      onSubmitEditing={() => this.details.focus()}
    />
  }
}